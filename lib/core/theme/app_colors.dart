import 'package:flutter/material.dart';

class AppColors {
  // Primary — Soft Indigo (내향형 유저에게 편안한 톤)
  static const primary         = Color(0xFF5B67CA);
  static const primaryLight    = Color(0xFFDFE1FF);

  // Secondary — Soft Coral (따뜻한 연결감)
  static const secondary       = Color(0xFFFF7E67);

  // Background
  static const background      = Color(0xFFF5F6FF);
  static const surface         = Color(0xFFFFFFFF);
  static const surfaceVariant  = Color(0xFFEEEFF8);

  // Dark mode
  static const darkBackground  = Color(0xFF1A1B2E);
  static const darkSurface     = Color(0xFF252640);

  // Text
  static const textPrimary     = Color(0xFF1A1B2E);
  static const textSecondary   = Color(0xFF6B7280);
  static const textHint        = Color(0xFF9CA3AF);

  // Status
  static const error           = Color(0xFFE53935);
  static const success         = Color(0xFF43A047);

  // 소셜 로그인
  static const kakaoYellow     = Color(0xFFFEE500);
}