import 'package:flutter/material.dart';
class FootprintsScreen extends StatelessWidget {
  const FootprintsScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('발자취')));
}